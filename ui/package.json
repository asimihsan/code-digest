{
  "name": "code-digest-ui",
  "version": "1.0.0",
  "description": "Your new jumpstart project includes basic organization with an organized `assets` folder and a `components` folder.  If you chose to develop with the router feature, you will also have a `views` folder.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:css": "npx postcss assets/styling/main.css -o assets/styling/tailwind.css --watch"
  },
  "author": "",
  "license": "MPL-2.0",
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.6",
    "@tailwindcss/postcss": "^4.1.6",
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "postcss-cli": "^11.0.1",
    "tailwindcss": "^4.1.6"
  }
}
