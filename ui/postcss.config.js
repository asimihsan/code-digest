module.exports = {
  plugins: {
    "@tailwindcss/postcss": {},   // the new v4 plugin
    /* autoprefixer is optional in v4,
       include it only if you target legacy browsers */
    // autoprefixer: {},
  },
};
